import AppLayout from '@/components/AppLayout';
import SupportLocatorClient from './SupportLocatorClient';

export default function SupportLocatorPage() {
  return (
    <AppLayout>
      <SupportLocatorClient />
    </AppLayout>
  );
}